
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/mark1
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update   # for dev; use validate in prod
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

# JWT custom props
app:
  jwt:
    secret: ${JWT_SECRET:mySecretKey123456789012345678901234567890}
    expiration-ms: 86400000   # 1 day

server:
  port: 6003